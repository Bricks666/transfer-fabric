export const Sample = ({ name, categoryName, moneyCount }) => {
	return (
		<article>
			<p>Name: {name}</p>
			<p>Category: {categoryName}</p>
			<p>Money count: {moneyCount}</p>
		</article>
	);
};
